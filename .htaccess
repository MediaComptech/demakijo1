<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Mencegah browsing direktori secara langsung
    Options -Indexes

    # Kirim semua request ke folder public/ secara transparan
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
